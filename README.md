Client (React + Vite + Tailwind)
--------------------------------

1. Install and run:
   - npm install
   - npm run dev

2. The Vite dev server proxies /api to http://localhost:4000 (server).
3. Open http://localhost:5173 in your browser.
